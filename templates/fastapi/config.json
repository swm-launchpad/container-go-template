{
  "default_env": [
    {
      "key": "PYTHONUNBUFFERED",
      "value": "1"
    }
  ],
  "default_ports": [
    {
      "internal_port": 8000,
      "network_type": "http"
    }
  ],
  "template_options": [
    {
      "default": "3.11",
      "label": "Python 버전",
      "name": "python_version",
      "options": [
        "3.9",
        "3.10",
        "3.11",
        "3.12"
      ],
      "type": "select",
      "category": "required"
    },
    {
      "default": "pip",
      "label": "의존성 매니저",
      "name": "dependency_manager",
      "options": [
        "pip",
        "poetry",
        "pipenv"
      ],
      "type": "select",
      "category": "required"
    },
    {
      "default": "8000",
      "label": "애플리케이션 포트",
      "name": "app_port",
      "options": [],
      "type": "number",
      "category": "optional",
      "description": "FastAPI 서버가 실행될 포트 번호. ASGI 서버 실행 시 이 포트로 바인딩되며, 네트워크 포트와 자동으로 동기화됩니다."
    },
    {
      "label": "애플리케이션 모듈",
      "name": "app_module",
      "options": [
        "main:app",
        "app:app",
        "api:app",
        "src.main:app"
      ],
      "type": "select",
      "category": "required",
      "allow_custom": true,
      "custom_label": "기타(직접 입력)",
      "description": "FastAPI 앱 객체의 위치를 선택하세요. 예: main.py에 app = FastAPI()가 있으면 'main:app', src/api/app.py에 있으면 'src.api.app:app'"
    },
    {
      "default": "uvicorn",
      "label": "ASGI 서버",
      "name": "asgi_server",
      "options": [
        "uvicorn",
        "hypercorn"
      ],
      "type": "select",
      "category": "advanced",
      "description": "ASGI 애플리케이션 서버. Uvicorn은 빠른 성능, Hypercorn은 HTTP/2 지원."
    },
    {
      "default": "1",
      "label": "워커 프로세스 수",
      "name": "workers",
      "options": [],
      "type": "number",
      "category": "advanced",
      "description": "동시 요청 처리를 위한 워커 프로세스 수. CPU 코어 수와 동일하게 설정 권장."
    }
  ],
  "template_ports": [
    {
      "description": "FastAPI server port",
      "internal_port": 8000,
      "network_type": "http"
    }
  ]
}