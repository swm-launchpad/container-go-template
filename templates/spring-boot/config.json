{
  "default_env": [
    {
      "key": "SERVER_PORT",
      "value": "8080"
    }
  ],
  "default_ports": [
    {
      "internal_port": 8080,
      "network_type": "http"
    }
  ],
  "template_options": [
    {
      "default": "17",
      "label": "Java 버전",
      "name": "java_version",
      "options": [
        "17",
        "21"
      ],
      "type": "select",
      "category": "required"
    },
    {
      "default": "gradle",
      "label": "빌드 도구",
      "name": "build_tool",
      "options": [
        "maven",
        "gradle"
      ],
      "type": "select",
      "category": "required"
    },
    {
      "default": "3.9",
      "label": "Maven 버전",
      "name": "maven_version",
      "options": [
        "3.9"
      ],
      "type": "select",
      "category": "required"
    },
    {
      "default": "8.5",
      "label": "Gradle 버전",
      "name": "gradle_version",
      "options": [
        "8.5"
      ],
      "type": "select",
      "category": "required"
    },
    {
      "default": "prod",
      "label": "Spring Profile",
      "name": "spring_profile",
      "options": [
        "dev",
        "prod",
        "test"
      ],
      "type": "select",
      "category": "optional"
    },
    {
      "default": "8080",
      "label": "애플리케이션 포트",
      "name": "app_port",
      "options": [],
      "type": "number",
      "category": "optional"
    }
  ],
  "template_ports": [
    {
      "description": "Spring Boot server port",
      "internal_port": 8080,
      "network_type": "http"
    }
  ]
}