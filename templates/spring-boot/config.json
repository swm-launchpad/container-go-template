{
  "default_resources": {
    "default_cpu": 1000,
    "default_memory": 1024,
    "min_recommended_memory": 1024
  },
  "default_env": [],
  "port_guide": {
    "default_port": 8080,
    "description": "Spring Boot는 기본적으로 8080번 포트를 사용합니다. 설정에서 server.port를 변경한 경우 해당 포트를 추가하세요."
  },
  "template_options": [
    {
      "default": "17",
      "label": "Java 버전",
      "name": "java_version",
      "options": [
        "11",
        "17",
        "21"
      ],
      "type": "select",
      "category": "required"
    },
    {
      "default": "gradle",
      "label": "빌드 도구",
      "name": "build_tool",
      "options": [
        "maven",
        "gradle"
      ],
      "type": "select",
      "category": "required"
    },
    {
      "default": "3.9",
      "label": "Maven 버전",
      "name": "maven_version",
      "options": [
        "3.9"
      ],
      "type": "select",
      "category": "advanced",
      "description": "Maven 빌드 도구 버전. 특별한 이유가 없다면 기본값 사용을 권장합니다."
    },
    {
      "default": "8.5",
      "label": "Gradle 버전",
      "name": "gradle_version",
      "options": [
        "8.5"
      ],
      "type": "select",
      "category": "advanced",
      "description": "Gradle 빌드 도구 버전. 특별한 이유가 없다면 기본값 사용을 권장합니다."
    },
    {
      "default": "prod",
      "label": "Spring Profile",
      "name": "spring_profile",
      "options": [
        "dev",
        "prod",
        "test"
      ],
      "type": "select",
      "category": "optional"
    },
    {
      "default": "75",
      "label": "JVM 최대 힙 메모리 비율",
      "name": "jvm_max_ram_percentage",
      "options": [
        "50",
        "60",
        "70",
        "75",
        "80",
        "90"
      ],
      "type": "select",
      "category": "advanced",
      "allow_custom": true,
      "custom_label": "기타(직접 입력)",
      "description": "컨테이너 메모리 대비 JVM 최대 힙 메모리 비율(%). 75% 권장 (나머지는 non-heap, OS용)"
    },
    {
      "default": "50",
      "label": "JVM 초기 힙 메모리 비율",
      "name": "jvm_initial_ram_percentage",
      "options": [
        "25",
        "33",
        "50",
        "60",
        "75"
      ],
      "type": "select",
      "category": "advanced",
      "allow_custom": true,
      "custom_label": "기타(직접 입력)",
      "description": "컨테이너 메모리 대비 JVM 초기 힙 메모리 비율(%). 50% 권장 (빠른 시작)"
    }
  ]
}
